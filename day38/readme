1、粘包的根本原因在于tcp数据包是流式的,分不清数据的开头和结束
2、粘包既可以发生在客户端(对于数据量小且间隔时间短的数据会一起发送,这样网络延迟就只发生了一次了),
也可以发生在服务端(服务端取数据的时候不清楚取多少数据,如果第一次发的数据是2000,第一次取1024,第二次取1024的时候就会取得第一次还没取完的数据)
3、粘包的解决方式是加一个"协议头",我们开发的是应用层的程序,所以我们需要定制自己的协议,我们现在仅仅把数据发送了,并没有发送我们自己的头,
注意:这里的协议头是我们自己定义的,长度啥的也是自己预先规定好的,这样两端达成统一就好处理了